# A1Z LeRobot 指令索引

所有命令都从以下目录执行：

```bash
conda activate lerobot-a1z
cd /home/anno/learning/A1Z_Lerobot/a1z-lerobot
```

## 通用检查

```bash
lerobot-find-port
lerobot-find-cameras realsense
bash a1z_lerobot/scripts/setup.sh can0
ip -details link show can0
```

双臂再执行：

```bash
bash a1z_lerobot/scripts/setup.sh can1
ip -details link show can1
```

## 单臂

校准：

```bash
a1z-calibrate-leader --port=/dev/ttyACM0 --id=a1z_leader
```

无相机遥控、双 RGB 录制、ACT 训练和 ACT 推理的完整已验收参数见
[README 单臂章节](README.md#单臂-a1z七轴示教双-realsenseact)。对应入口为：

```bash
a1z-teleoperate-single --help
a1z-record-single --help
a1z-train-act-single --help
a1z-rollout-act-single --help
```

默认单臂配置：

```text
a1z_lerobot/configs/record_a1z_single_realsense.yaml
a1z_lerobot/configs/a1z_single_realsense.yaml
```

## 双臂

左右 Leader 独立校准：

```bash
a1z-calibrate-leader --port=/dev/ttyACM0 --id=a1z_left_leader
a1z-calibrate-leader --port=/dev/ttyACM1 --id=a1z_right_leader
```

双臂入口：

```bash
a1z-teleoperate-dual --help
a1z-record-dual --help
a1z-train-act-dual --help
a1z-rollout-act-dual --help
```

默认双臂配置：

```text
a1z_lerobot/configs/record_a1z_dual_realsense.yaml
a1z_lerobot/configs/a1z_dual_realsense.yaml
```

第二台 D405 的序列号必须传入：

```bash
RIGHT_D405_SERIAL=在这里填右腕D405序列号
```

完整的五秒无相机遥控命令、三路 RGB Rerun 验证、正式录制、重录快捷键、ACT 训练、
十秒推理和更换整套硬件流程见 [A1Z 双臂操作手册](docs/a1z-dual-arm-commands.md)。

## 录制快捷键

- `→`：提前结束当前阶段。
- `←`：丢弃当前尚未保存的一集并重录。
- `Esc`：停止录制并 finalize 已保存数据。

新数据集应使用尚不存在的目录；继续同一数据集才使用 `--resume=true`。不要让训练或
推理配置的相机键、分辨率和状态/动作维度与录制数据不一致。
