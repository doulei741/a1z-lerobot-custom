# Third-party assets

No external photographs, robot renders, fonts, or hot-linked assets are included. The UI uses only CSS surfaces and icons from the `lucide-react` package (ISC License). Package provenance and exact resolved versions are recorded in `frontend/pnpm-lock.yaml`.
